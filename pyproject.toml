[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "proseg-reference-tests"
version = "0.0.0"
requires-python = ">=3.13,<3.14"
dependencies = []

[dependency-groups]
dev = [
  "build==1.3.0",
  "numpy==2.5.1",
  "pytest==9.0.2",
  "ruff==0.13.2",
  "scipy==1.18.0",
  "shapely==2.1.2",
  "wheel==0.45.1",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
